---
name: cco-on
description: Turn AI Cost Optimizer back on for this project.
---

# /cco-on

Run `node <plugin>/scripts/cco-init.mjs --workspace . --enable` and confirm in one line. If the project was never set up (no `.cursor/agents/cco-fast.md`), run `/cco-init` instead.
