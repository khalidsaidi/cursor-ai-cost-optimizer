---
name: cco-off
description: Turn AI Cost Optimizer off for this project (keeps files; run /cco-on to re-enable).
---

# /cco-off

Run `node <plugin>/scripts/cco-init.mjs --workspace . --disable` and confirm in one line that CCO is off for this project. Remind the user that `[cco:off]` in a single prompt does the same for one request, and `/cco-on` re-enables.
