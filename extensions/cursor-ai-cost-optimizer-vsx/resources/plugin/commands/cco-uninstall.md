---
name: cco-uninstall
description: Remove AI Cost Optimizer from this project (its hook entries, tier subagents, settings and state under .cursor/); other hooks and your own agents are kept.
---

# /cco-uninstall

Run `node <plugin>/scripts/cco-init.mjs --workspace . --uninstall`, show the list of removed files, and remind the user that the plugin itself is removed from Cursor's plugin settings.
